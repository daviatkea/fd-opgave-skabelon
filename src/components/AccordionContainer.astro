---
import Accordion from "../components/Accordion.astro";

const { data, exclusive } = Astro.props;
---

<section class="accordion-container">
  <div>
    {data.map((props) => <Accordion {...props} {exclusive} />)}
  </div>
</section>

<style>
  section {
    padding-block: 3rem;
    background: #f5f5f5;
  }
  div {
    padding: 1rlh;
    max-width: 725px;
    margin-inline: auto;
  }
</style>
