<header>
  <div class="header-content">
    <h1 class="header-title">AskExperts</h1>
    <nav>
      <ul class="nav-list">
        <li><a href="#home" class="nav-link">Home</a></li>
        <li><a href="#about" class="nav-link">Case Study</a></li>
        <li><a href="#services" class="nav-link">Team</a></li>
        <li><a href="#contact" class="nav-link">About</a></li>
      </ul>
    </nav>

    <button popovertarget="popup" class="cta-button">Login</button>
    <div popover id="popup" class="login-popup">
      <h2 class="popup-title">Dashboard Login</h2>
      <div class="form-container">
        <div class="form-field">
          <label for="email" class="field-label">Email</label>
          <input type="email" id="email" class="field-input" placeholder="Din email..." />
        </div>
        <div class="form-field">
          <label for="password" class="field-label">Password</label>
          <input type="password" id="password" class="field-input" placeholder="Dit kodeord..." />
        </div>
      </div>
      <button type="submit" class="submit-button">Go to dashboard</button>
    </div>
  </div>
</header>
<style>
  @layer components {
    :where(header) {
      --header-bg: rgba(27, 27, 27, 0.91);
      --header-blur: 10.5px;
      --header-padding: clamp(1rem, 3vw, 2.375rem);
      --header-gap: clamp(1rem, 2vw, 1.25rem);
    }
  }

  header {
    background: var(--header-bg);
    backdrop-filter: blur(var(--header-blur));
    display: flex;
    justify-content: center;
    position: sticky;
    top: 0;
    z-index: 100;

    contain: layout style;
    isolation: isolate;
  }

  .header-content {
    display: flex;
    padding-block: var(--header-padding);
    justify-content: space-between;
    align-items: center;
    gap: var(--header-gap);
    width: 100%;
    max-width: var(--header-max-width);

    min-height: 4rem;
    container-type: inline-size;
    container-name: header;
  }

  .header-title {
    color: var(--secondary-white);
    font-family: Cabin, sans-serif;
    font-size: var(--h1);
    font-weight: 600;
    line-height: 1.05;

    text-wrap: balance;
    overflow-wrap: break-word;
  }

  .nav-list {
    list-style: none;
    display: flex;
    gap: var(--flow-md);
    margin: 0;
    padding: 0;

    flex-wrap: wrap;
    align-items: center;
  }

  .nav-link {
    color: var(--secondary-white);
    text-decoration: none;
    font-weight: 500;
    padding: var(--flow-xs) var(--flow-sm);
    border-radius: var(--radius-sm);

    min-height: 2.75rem;
    display: inline-flex;
    align-items: center;

    position: relative;

    &:hover,
    &:focus-visible {
      text-decoration: underline;
      text-decoration-thickness: 1.5px;
      text-underline-offset: 0.25em;s
    }

    &:focus-visible {
      outline: 2px solid var(--primary-yellow);
      outline-offset: 2px;
    }
  }

  button {
    border: 0;
    border-radius: var(--radius-md);
    background: var(--primary-yellow);
    color: var(--primary-black);
    font-family: Lato, system-ui, sans-serif;
    font-size: var(--p);
    font-weight: 400;
    line-height: 1.6;

    display: inline-flex;
    padding: var(--flow-xs) var(--flow-md);
    justify-content: center;
    align-items: center;
    gap: var(--flow-sm);

    cursor: pointer;
    transition:
      background-color var(--transition-fast),
      transform var(--transition-fast);
    white-space: nowrap;
    user-select: none;

    &:hover {
      background: var(--primary-yellow-hover);
    }

    &:focus-visible {
      outline: 2px solid var(--primary-black);
      outline-offset: 2px;
    }
  }

  .login-popup {
    display: flex;
    padding: var(--flow-xl) var(--flow-xl);
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: var(--flow-sm);

    border: 0;
    border-radius: var(--radius-lg);
    background: var(--primary-black);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);

    
    opacity: 0;
    translate: 0 -100%;
   
    transition: var(--transition-normal);
    transition-property: var(--transition-props-popup);
    transition-behavior: allow-discrete;

    
    max-width: 90vw;
    max-height: 90vh;
    overflow: auto;
    overscroll-behavior: contain;
  }

  .popup-title {
    color: var(--secondary-white);
    text-align: center;
    font-family: Cabin, system-ui, sans-serif;
    font-size: var(--h2);
    font-weight: 700;
    line-height: 1.16;
    margin: 0 0 var(--flow-lg) 0;

    
    text-wrap: balance;
  }

  .form-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--flow-lg);
    width: 100%;
    max-width: 20rem;
  }

  .form-field {
    display: flex;
    flex-direction: column;
    gap: var(--flow-xs);
    width: 100%;
  }

  .field-label {
    color: var(--secondary-white);
    font-weight: 500;
    font-size: var(--p);
  }

  .field-input {
    background: var(--secondary-white);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 0.75rem 1rem;
    width: 100%;
    font-size: var(--p);
    color: var(--primary-black);
    font-family: Lato, system-ui, sans-serif;
    transition:
      border-color var(--transition-fast),
      box-shadow var(--transition-fast);

    &:focus {
      outline: none;
      border-color: var(--primary-yellow);
      box-shadow: var(--focus-ring);
    }

    &::placeholder {
      color: #666;
      opacity: 1;
    }
  }

  .login-popup:popover-open {
    opacity: 1;
    translate: 0 0;

    @starting-style {
      opacity: 0;
      translate: 0 -100%;
    }
  }

  .login-popup::backdrop {
    background: var(--backdrop-overlay);
    backdrop-filter: blur(16px);

    
    opacity: 0;
    backdrop-filter: blur(0px);

    transition: var(--transition-normal);
    transition-property: var(--transition-props-backdrop);
    transition-behavior: allow-discrete;

    @starting-style {
      opacity: 0;
      backdrop-filter: blur(0px);
    }
  }

  .login-popup:popover-open::backdrop {
    opacity: 1;
    backdrop-filter: blur(16px);
  }

  /* container query */
  @container header (max-width: 768px) {
    nav {
      display: none;
    }
  }
</style>
