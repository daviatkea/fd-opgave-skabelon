<header>
  <div class="header-content">
    <h1 class="header-title">AskExperts</h1>
    <nav>
      <ul class="nav-list">
        <li><a href="/" class="nav-link">Home</a></li>
        <li><a href="/case" class="nav-link">Case Study</a></li>
        <li><a href="/team" class="nav-link">Team</a></li>
        <li><a href="/about" class="nav-link">About</a></li>
      </ul>
    </nav>

    <button popovertarget="popup" class="cta-button">Login</button>
    <div popover id="popup" class="login-popup">
      <h2 class="popup-title">Dashboard Login</h2>
      <div class="form-container">
        <div class="form-field">
          <label for="email" class="field-label">Email address</label>
          <input type="email" id="email" class="field-input" />
        </div>
        <div class="form-field">
          <label for="password" class="field-label">Password</label>
          <input type="password" id="password" class="field-input" />
        </div>
        <button type="button" class="submit-button">Go to dashboard</button>
      </div>
    </div>
  </div>
</header>
<style>
  @layer components {
    header {
      background: rgba(27, 27, 27, 0.91);
      backdrop-filter: blur(10.5px);
      display: flex;
      justify-content: center;
      position: sticky;
      top: 0;
      z-index: 100;
      contain: layout style;
      isolation: isolate;
      padding-inline: var(--flow-md);
    }

    .header-content {
      container-type: inline-size;
      container-name: header;
      display: flex;
      padding-block: var(--flow-md);
      justify-content: space-between;
      align-items: center;
      gap: var(--flow-md);
      width: 100%;
      max-width: var(--header-max-width);
      min-height: 4rem;
    }

    .header-title {
      color: var(--secondary-white);
      font-family: var(--font-cabin);
      font-size: var(--h2);
      font-weight: 600;
      line-height: 1.16;
      margin: 0;
      text-wrap: balance;
      overflow-wrap: break-word;
    }

    nav {
      display: none;
    }

    .nav-list {
      list-style: none;
      display: flex;
      gap: var(--flow-md);
      margin: 0;
      padding: 0;
      flex-wrap: wrap;
      align-items: center;
    }

    .nav-link {
      color: var(--secondary-white);
      text-decoration: none;
      font-size: var(--p);
      font-weight: 500;
      padding: var(--flow-xs) var(--flow-sm);
      border-radius: var(--radius-sm);
      min-height: 2.75rem;
      display: inline-flex;
      align-items: center;
      position: relative;
      transition: text-decoration var(--transition-fast);

      &:hover,
      &:focus-visible {
        text-decoration: underline;
        text-decoration-thickness: 1.5px;
        text-underline-offset: 0.25em;
      }

      &:focus-visible {
        outline: 2px solid var(--primary-yellow);
        outline-offset: 2px;
      }
    }

    .cta-button,
    .submit-button {
      border: 0;
      border-radius: var(--border-radius);
      background: var(--primary-yellow);
      color: var(--primary-black);
      font-family: var(--font-lato);
      font-size: var(--p);
      font-weight: 400;
      line-height: 1.6;
      display: inline-flex;
      padding: var(--flow-sm) var(--flow-lg);
      justify-content: center;
      align-items: center;
      gap: var(--flow-sm);
      cursor: pointer;
      transition:
        background-color var(--transition-fast),
        transform var(--transition-fast);
      white-space: nowrap;
      user-select: none;

      &:hover {
        background: var(--primary-yellow-hover);
        transform: scale(1.02);
      }

      &:focus-visible {
        outline: 2px solid var(--primary-yellow);
        outline-offset: 2px;
      }
    }

    .login-popup {
      display: flex;
      padding: 5rem 6.25rem;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 2.875rem;
      border: 0;
      border-radius: 1.875rem;
      background: var(--primary-black);
      box-shadow: var(--shadow-card);
      opacity: 0;
      translate: 0 -100%;
      transition: var(--transition-normal);
      transition-property: var(--transition-props-popup);
      transition-behavior: allow-discrete;
      max-width: 90vw;
      max-height: 90vh;
      overflow: auto;
      overscroll-behavior: contain;
    }

    .popup-title {
      color: var(--secondary-white);
      text-align: center;
      font-family: var(--font-cabin);
      font-size: 2.8125rem;
      font-weight: 700;
      line-height: 1.16;
      margin: 0;
      text-wrap: balance;
      white-space: nowrap;
    }

    .form-container {
      display: flex;
      flex-direction: column;
      align-items: stretch;
      gap: 1.875rem;
      width: 100%;
      max-width: 330px;
    }

    .form-field {
      display: flex;
      flex-direction: column;
      gap: 0.3125rem;
      width: 100%;
    }

    .field-label {
      color: var(--secondary-white);
      font-family: var(--font-cabin);
      font-weight: 400;
      font-size: 1.125rem;
      line-height: 1.16;
      white-space: nowrap;
    }

    input {
      background: var(--secondary-white);
      border: 1px solid #e3e3e3;
      border-radius: var(--radius-xs);
      padding: 0;
      width: 100%;
      height: 3.6875rem;
      font-size: var(--p);
      color: var(--primary-black);
      font-family: var(--font-lato);
      padding-inline: 1rem;
      transition:
        border-color var(--transition-fast),
        box-shadow var(--transition-fast);

      &:focus {
        outline: none;
        border-color: var(--primary-yellow);
        box-shadow: var(--focus-ring);
      }

      &::placeholder {
        color: var(--primary-black);
        opacity: 1;
      }
    }

    .submit-button {
      width: 100%;
      padding: 0.9375rem 3.5rem;
      font-size: 1.125rem;
      font-weight: 600;
      margin-top: 0;
    }

    .login-popup:popover-open {
      opacity: 1;
      translate: 0 0;

      @starting-style {
        opacity: 0;
        translate: 0 -100%;
      }
    }

    .login-popup::backdrop {
      background: var(--backdrop-overlay);
      backdrop-filter: blur(16px);
      opacity: 0;
      backdrop-filter: blur(0px);
      transition: var(--transition-normal);
      transition-property: var(--transition-props-backdrop);
      transition-behavior: allow-discrete;

      @starting-style {
        opacity: 0;
        backdrop-filter: blur(0px);
      }
    }

    .login-popup:popover-open::backdrop {
      opacity: 1;
      backdrop-filter: blur(16px);
    }

    @container header (width > 1199px) {
      header {
        padding-inline: 0;
      }

      .header-content {
        padding-block: var(--flow-lg);
      }

      .header-title {
        font-size: var(--h1);
      }

      nav {
        display: block;
      }
    }
  }
</style>
